export function writePerson(person){
    console.log(`first ${person.first}, last ${person.lastName}, age : ${person.age}`);
}

// --- numric sum : add numbers
export function sum (n1, n2){
    return n1+n2;
}